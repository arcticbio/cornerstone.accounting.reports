Previous page: section_id={{ previous.section_id if previous else "none" }}, continuation={{ previous.is_continuation | lower if previous else "false" }}, record_qualifier={{ previous.record_qualifier if previous and previous.record_qualifier else "null" }}
Page {{ page }} of {{ page_count }} of document role {{ doc_role }}.
